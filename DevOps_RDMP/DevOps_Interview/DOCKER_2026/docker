Basic commands of DOcker

1. docker run -it --rm ubuntu bash 
 example: docker run -it --rm ubuntu bash (it: interactive mode, rm: remove the container after exit, ubuntu: image name, bash: command to run inside the container)
